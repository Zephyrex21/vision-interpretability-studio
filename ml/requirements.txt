# Local dev / CI dependencies for ml/src (math tests only — no GPU needed)
numpy>=1.26
pytest>=8.0

# Full notebook dependencies (Kaggle's environment already provides these;
# only needed if you want to run outside Kaggle, e.g. locally with a GPU)
torch>=2.2
torchvision>=0.17
onnx>=1.16
onnxruntime>=1.17
matplotlib>=3.8
